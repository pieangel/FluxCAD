<UserControl x:Class="FluxCAD.Gui26.Views.FluxCadPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Width="160" Height="32" Content="Analyze Active DWG" Click="OnAnalyzeClick"/>
            <Button Width="100" Height="32" Content="Analyze Active DWG" Click="OnStripDimsClick"/>
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Text="{Binding Header}"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,8,0,8" Text="{Binding TotalText}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="By Type" Margin="0,0,8,0">
                <DataGrid ItemsSource="{Binding ByType}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="By Layer (Top)" Margin="8,0,0,0">
                <DataGrid ItemsSource="{Binding ByLayer}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Layer" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
